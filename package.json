{
  "name": "rete-connection-plugin",
  "version": "0.3.1",
  "description": "",
  "main": "build/connection-plugin.min.js",
  "scripts": {
    "build": "rete --build rete.config.js",
    "build:dev": "rete --build rete.config.js --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "rete-cli": "^0.2.0",
    "rollup-plugin-sass": "^0.6.1"
  }
}
